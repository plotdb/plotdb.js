# old

 * data.ls
   * data
     - sample
       - country
       - continent
       - category
       - name
       - fruit
       - weekday
       - month
       - generate: ->
       - crimean-war
       - life-expectancy
       - old-faithful
       - some of them got injected with helper.
 * theme.ls
   * theme ( deprecated )
 * type.ls
   * String
   * Order
   * ...
 * util.ls
   * util.trackResizeEvent
 * main.ls
   define plotdb object. add some polyfill
 * config.ls
   * config
     - language
     - fontFamily
     - ...
 * chart.ls
   * chart
     - corelib
     - create
     - base
     - fields-from-dimension
     - data-from-dimension
     - data-convert
       - from-dimension
       - by-mapping
     - data-from-hash
     - get-sample-data
     - update-data
     - update-dimension
     - update-assets
     - update-config
     - add
     - get
     - list
     - config
       - update
       - preset
       - parse
 * view.ls
   * view
     - host
     - loader
     - chart ( constructor )
       chart member function:
       - update
       - loadlib
       - attach
       - config
       - init
       - parse
       - resize
       - bind
       - render
       - destroy
       - clone
       - on
       - theme
       - refresh
       - data
       - sync
